---
- name: Install zip
  pacman:
    name: "{{ item }}"
    state: present
  loop:
    - zip
    - unzip

- name: Install password manager
  pacman:
    name: keepassxc
    state: present

- name: Install gimp photo editor
  pacman:
    name: gimp
    state: present
